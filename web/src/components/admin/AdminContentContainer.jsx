"use client";

/**
 * Standard admin page content column — sits below header + horizontal nav + search.
 */
export default function AdminContentContainer({ children }) {
  return <div className="adminContentContainer">{children}</div>;
}
