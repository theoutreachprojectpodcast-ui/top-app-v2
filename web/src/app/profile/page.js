import AppShell from "@/components/layout/AppShell";

export default function ProfilePage() {
  return (
    <AppShell activeNav="profile">
      <section className="panel">
        <h1>Profile</h1>
        <p>
          Profile and membership features will move here in Phase 3 and data/state
          extraction will follow in Phase 4.
        </p>
      </section>
    </AppShell>
  );
}

