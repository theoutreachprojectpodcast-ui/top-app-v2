import AppShell from "@/components/layout/AppShell";

export default function TrustedPage() {
  return (
    <AppShell activeNav="trusted">
      <section className="panel">
        <h1>Trusted Resources</h1>
        <p>
          Placeholder route created for migration. Existing trusted-resource behavior
          will be ported in Phase 3 with full parity.
        </p>
      </section>
    </AppShell>
  );
}
