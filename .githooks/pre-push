#!/bin/sh
# Block pushes when gh is not authenticated as theoutreachprojectpodcast-ui for this repo.
set -eu

ROOT="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"
node "$ROOT/scripts/verify-github-remote.mjs"
